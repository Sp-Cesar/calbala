<div class="relative w-full h-screen bg-gray-50 overflow-hidden">
    
    <!-- Area del Gráfico (Full Screen) -->
    <div id="canvas-container" class="absolute inset-0 w-full h-full z-0"></div>

    <!-- Referencia de Coordenadas (Floating) -->
    <div id="coords-display" class="absolute top-4 left-4 text-gray-700 p-2 bg-white/80 backdrop-blur-sm rounded-lg text-xs font-mono border border-gray-200 shadow-sm z-10 pointer-events-none">
        r: 0.00, &theta;: 0.00°
    </div>

    <!-- Sidebar Partial -->
    <%- include('../partials/sidebar') %>

</div>